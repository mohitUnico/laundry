import 'package:flutter/material.dart';

class DistributionManagerHomeScreen extends StatelessWidget {
  const DistributionManagerHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Distribution Manager')),
      body: const Center(
        child: Text('Distribution manager dispatch - To be implemented'),
      ),
    );
  }
}
