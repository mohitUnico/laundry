import 'package:flutter/material.dart';

class ServiceManHomeScreen extends StatelessWidget {
  const ServiceManHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Service Man')),
      body: const Center(
        child: Text('Service man queue - To be implemented'),
      ),
    );
  }
}
