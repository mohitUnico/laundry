<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local dev: backend on http://<LAN_IP>:3001 (REST + SSE assignment stream) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
