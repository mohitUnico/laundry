import 'package:flutter/material.dart';

class CollectionManagerHomeScreen extends StatelessWidget {
  const CollectionManagerHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Collection Manager')),
      body: const Center(
        child: Text('Collection manager dashboard - To be implemented'),
      ),
    );
  }
}
